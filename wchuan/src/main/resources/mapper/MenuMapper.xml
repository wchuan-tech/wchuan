<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wchuan.system.mapper.MenuMapper">

    <select id="selectMenuNameByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.`perms`
        FROM
            sys_user_role ur
                LEFT JOIN sys_role r ON ur.`role_id` = r.`id`
                LEFT JOIN sys_role_menu rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN sys_menu m ON m.`id` = rm.`menu_id`
        WHERE
            user_id = #{userId}
          AND r.`status` = 0
          AND m.`status` = 0;
    </select>

    <resultMap id="SysMenuResult" type="com.wchuan.system.domain.entity.SysMenu">
        <id     property="id"         column="id"        />
        <result property="menuName"   column="menu_name" />
        <result property="parentId"   column="parent_id" />
        <result property="orderNum"   column="order_num" />
        <result property="path"       column="path"      />
        <result property="component"  column="component" />
        <result property="perms"      column="perms"     />
        <result property="visible"    column="visible"   />
        <result property="status"     column="status"    />
        <result property="menuType"   column="menu_type" />
        <result property="icon"       column="icon"      />
        <result property="createTime" column="create_time" />
        <!-- 注意：children 字段通常标注为 @TableField(exist = false)，
             它是你在 Service 层递归生成的，SQL 查询时通常不需要映射它 -->
    </resultMap>

    <select id="selectMenuTreeByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.*
        FROM sys_menu m
                 LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
                 LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE
            ur.user_id = #{userId}
          AND m.status = 0
          AND m.menu_type IN ('M', 'C')
        ORDER BY m.parent_id, m.order_num
    </select>

</mapper>